DB_USER=developer
DB_PASSWORD=D@v@loper66
DB_HOST=192.168.10.148
DB_PORT=3306
JWT_SECRET=your_jwt_secret_key

# suppose we have currently stored 4 users like t1 to t4 in users table that thing we have already managed after this manage, we have create login api using then suppose when t2 user logged in then t1 , t3 and t4 users are shown that thing we have already managed using users table(already created) but in next phase i want to creat chat like suppose t1 logged in and when t2 t3 & t4 are shown then suppose add single user from user list then pls give me the table architecture in which table i wnat to store the chat user in which table we have initiated the chat. give complete process along with the code